Q41. Write a program to write a function to find the sum of two numbers.

Program
#include <stdio.h>

int sum(int a, int b)
{
    return a + b;
}

int main()
{
    int a, b;

    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    printf("Sum = %d", sum(a, b));

    return 0;
}

Output
Enter two numbers: 15 25
Sum = 40

Q42. Write a program to write a function to find the maximum of two numbers.

Program
#include <stdio.h>

int maximum(int a, int b)
{
    if (a > b)
        return a;
    else
        return b;
}

int main()
{
    int a, b;

    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    printf("Maximum = %d", maximum(a, b));

    return 0;
}

Output
Enter two numbers: 25 18
Maximum = 25

Q43. Write a program to write a function to check whether a number is prime or not.

Program
#include <stdio.h>

int isPrime(int n)
{
    int i;

    if (n <= 1)
        return 0;

    for (i = 2; i <= n / 2; i++)
    {
        if (n % i == 0)
            return 0;
    }

    return 1;
}

int main()
{
    int n;

    printf("Enter a number: ");
    scanf("%d", &n);

    if (isPrime(n))
        printf("%d is a Prime Number.", n);
    else
        printf("%d is Not a Prime Number.", n);

    return 0;
}

Output
Enter a number: 17
17 is a Prime Number.

Q44. Write a program to write a function to find the factorial of a number.

Program
#include <stdio.h>

long long factorial(int n)
{
    long long fact = 1;
    int i;

    for (i = 1; i <= n; i++)
    {
        fact *= i;
    }

    return fact;
}

int main()
{
    int n;

    printf("Enter a number: ");
    scanf("%d", &n);

    printf("Factorial = %lld", factorial(n));

    return 0;
}

Output
Enter a number: 5
Factorial = 120